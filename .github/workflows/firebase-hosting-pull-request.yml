# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy Frontend To Firebase Hosting On PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    environment: Mail Envs
    env:
      NEXT_FIREBASE_API_KEY: ${{ secrets.NEXT_FIREBASE_API_KEY }}
      NEXT_FIREBASE_AUTH_DOMAIN: ${{ secrets.NEXT_FIREBASE_AUTH_DOMAIN }}
      NEXT_FIREBASE_PROJECT_ID: ${{ secrets.NEXT_FIREBASE_PROJECT_ID }}
      NEXT_FIREBASE_STORAGE_BUCKET: ${{ secrets.NEXT_FIREBASE_STORAGE_BUCKET }}
      NEXT_FIREBASE_SENDER_ID: ${{ secrets.NEXT_FIREBASE_SENDER_ID }}
      NEXT_FIREBASE_APP_ID: ${{ secrets.NEXT_FIREBASE_APP_ID }}
      NEXT_FIREBASE_MEASUREMENT_ID: ${{ secrets.NEXT_FIREBASE_MEASUREMENT_ID }}
    steps:
      - uses: actions/checkout@v2
      - name: Check environment variables
        run: |
          echo "NEXT_FIREBASE_API_KEY=$NEXT_FIREBASE_API_KEY"
          echo "NEXT_FIREBASE_AUTH_DOMAIN=$NEXT_FIREBASE_AUTH_DOMAIN"
          echo "NEXT_FIREBASE_PROJECT_ID=$NEXT_FIREBASE_PROJECT_ID"
          echo "NEXT_FIREBASE_STORAGE_BUCKET=$NEXT_FIREBASE_STORAGE_BUCKET"
          echo "NEXT_FIREBASE_SENDER_ID=$NEXT_FIREBASE_SENDER_ID"
          echo "NEXT_FIREBASE_APP_ID=$NEXT_FIREBASE_APP_ID"
          echo "NEXT_FIREBASE_MEASUREMENT_ID=$NEXT_FIREBASE_MEASUREMENT_ID"
          
      - run: npm install && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PIXELAI_30F7A }}'
          projectId: pixelai-30f7a
