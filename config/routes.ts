export const protectedRoutes: Record<string, { path: string }> = {
  dashboard: { path: "/dashboard" },
};

export const defaultRoutes: Record<string, { path: string }> = {
  home: { path: "/" },
  about: { path: "/about" },
  register: { path: "/register" },
};
